const DIR_DEVELOPMENT = 'reminders';

const DIR_PATH = {
	temp: 'build/temp',
	release: 'build/release',
	production: 'build/production' // minify version
};

module.exports = { DIR_DEVELOPMENT, DIR_PATH };
